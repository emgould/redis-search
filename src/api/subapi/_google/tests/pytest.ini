[pytest]
markers =
    unit: Unit tests that use mocked data (fast, no API calls)
    integration: Integration tests that make real API calls (slow, requires API key)

# Test discovery
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
console_output_style = progress

