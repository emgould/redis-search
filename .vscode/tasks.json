{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Load Secrets for Web Debug",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "cd '${workspaceFolder}' && source venv/bin/activate && export LOCAL_DEV=true && source scripts/load_secrets.sh local api && set -a && env > .env.debug"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}

